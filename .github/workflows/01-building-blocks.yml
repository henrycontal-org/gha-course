name: 01 - Building Blocks
on: 
  push:
    branches: [main]
  workflow_dispatch:
  
jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Hello, World!'

  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Failed step
        run: |
          echo 'I won't fail'
          exit 0

      - name: Say goodbye
        run: echo 'Goodbye!'
